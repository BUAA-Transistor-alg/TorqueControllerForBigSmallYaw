# 构建产物（由 build.sh 生成，不入库）
build/

# Python 缓存
__pycache__/
*.pyc

# 说明: data/ **全部入库**（沿用原仓库 TorqueController 的做法）——
#   data/targets/                     录制目标序列（辨识激励源）
#   data/cars/<车名>/                 每台车的标定结果与样本（见 data/cars/README.md）
#   data/archive/<日期>_<用途>/       历史归档（含仿真数据集与辨识结果）
#   data/sysid/                       当前工作区（正在采集/分析的数据）
# 只有 build/ 与 python 缓存不入库。

# 手写 C++ 快速前向（fast_sim）的编译产物：本机编译，不入库
python/scripts/identify_params/fast_sim/_build/
